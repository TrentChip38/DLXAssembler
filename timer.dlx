;Test the Timer by running and stopping after character entered
.text
	;Loop
	;prompt for integer
	TR
	TGO
	;Loop for a bit
	ADDI    R1, R0, 10000
OuterLoop
    SUBI    R1, R1, 1
    BNEZ    R1, OuterLoop
	TSP
end
	J 	end

